---
layout: 'base.njk'
---
<article>
    <header>
        <h1 class="articel__image">{{title}}</h1>
        <p>{{description}}</p>
    </header>
    <main>
<img src="{{image}}" alt="{{image_text}}" class="article__image">
{{content | safe}}

    </main>

</article>

