<li>
    <article class="snippet">
        <img src="{{post.data.image}}" alt="{{post.data.image_text}}" class="snippet__image horizontal"/>
            <h3 class="snippet__title"><a href="{{post.url}}">{{post.data.title}}</a></h3>
            <p class="snippet__body">{{post.data.description}}</p>
            <a href="{{post.url}}" class="btn">Weiterlesen</a>
    </article>
</li> 